# stac-fastapi-pgstac — the logbook API. Bare install, reachable in-cluster at http://stac-api.
# Reads/writes pgSTAC; CORS defaults to "*" (fine for the local demo, lets stac-browser call it).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stac-api
  namespace: eo
  labels:
    app: stac-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stac-api
  template:
    metadata:
      labels:
        app: stac-api
    spec:
      containers:
        - name: stac-api
          image: ghcr.io/stac-utils/stac-fastapi-pgstac@sha256:5b7e87c347d65a3a2a34673a0834aa3ec81835b49fd0f68147039826baa8165e
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: pgstac-credentials
                  key: POSTGRES_USER
            - name: POSTGRES_PASS
              valueFrom:
                secretKeyRef:
                  name: pgstac-credentials
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DBNAME
              valueFrom:
                secretKeyRef:
                  name: pgstac-credentials
                  key: POSTGRES_DB
            - name: POSTGRES_HOST_READER
              value: pgstac
            - name: POSTGRES_HOST_WRITER
              value: pgstac
            - name: POSTGRES_PORT
              value: "5432"
            - name: APP_HOST
              value: "0.0.0.0"
            # The image's launcher serves on 8080 regardless of APP_PORT; match it.
            - name: APP_PORT
              value: "8080"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /_mgmt/ping
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /_mgmt/ping
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: stac-api
  namespace: eo
  labels:
    app: stac-api
spec:
  selector:
    app: stac-api
  ports:
    - name: http
      port: 80
      targetPort: http
